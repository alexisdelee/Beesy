# general container of the database
basedir=database/

# max size by line to read
size_line=1000

# commit after creating a collection
initialization_commit=true

# create a log file after each connection
log=false